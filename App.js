import React from "react";
import { StyleSheet, Text, View } from "react-native";
import AppContainer from "./src/navigations/AppNavigation";

// App() that returns an AppContainer component. 
//The AppContainer component is likely defined elsewhere in the codebase.
//This code is likely written in JSX, which is a syntax extension for JavaScript that allows you to write HTML-like code in your JavaScript files.
export default function App() {
  return <AppContainer />;
}
// Test commit


//   git config --global user.email "you@example.com"
//    git config --global user.name "Your Name"/